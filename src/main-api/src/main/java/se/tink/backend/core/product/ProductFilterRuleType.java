package se.tink.backend.core.product;

public enum ProductFilterRuleType {
    AGE,
    CREDIT_SCORE,
    FEATURE_FLAG,
    LOCALE,
    MORTGAGE,
    MORTGAGE_AMOUNT,
    POSTAL_CODE,
    PRODUCT_CONSUMED,
    PROVIDER,
    PROVIDER_CAPABILITY,
    PROPERTY_TYPE,
    MONTHLY_SALARY
}
