package se.tink.backend.core;

public class BankFeeConstants {
    public static class Providers {

        // Key for "all-providers" which is used during grouping of data for all providers
        public static final String ALL_PROVIDERS = "all-providers";

        // Key for "unknown provider" which is used when we for some reason can't map a credential to a provider
        public static final String UNKNOWN_PROVIDER = "unknown-provider";
    }
}
