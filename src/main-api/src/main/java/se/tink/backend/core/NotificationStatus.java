package se.tink.backend.core;

public enum NotificationStatus {
    CREATED, SENT, SENT_ENCRYPTED, RECEIVED, READ;

    public static final String DOCUMENTED = "CREATED, SENT, SENT_ENCRYPTED, RECEIVED, READ";
}
