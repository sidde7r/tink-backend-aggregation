package se.tink.backend.rpc;

public enum SearchResponseMetricTypes {
    COUNT, 
    SUM, 
    AVG, 
    MONTHLY, 
    LAST_DATE, 
    FIRST_DATE, 
    VALID_PERIODS, 
    CATEGORIES, 
    NET, 
    STATISTICS
}
