/**
 * Package for business logic layer.
 * Take a note that role of controllers here is quite different from what is
 * called controllers in context of Spring framework, for example. They
 * roughly corresponds to what is usually called services in applications
 * build on top of Spring. Controllers here have nothing to do with HTTP
 * controllers in Rails as one of many web frameworks.
 */
package se.tink.backend.common.controllers;
