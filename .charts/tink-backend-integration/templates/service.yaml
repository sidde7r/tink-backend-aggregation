apiVersion: v1
kind: Service
metadata:
  name: integration-service
  namespace: integration
spec:
  selector:
    app: integration-service
  ports:
    - port: 443
      targetPort: https-api
      name: https-api
