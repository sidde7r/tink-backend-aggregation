name: tink-backend-aggregation-connectivity-cronjob
version: 1.0.0

tink:
  deployment:
    strategy: custom
    custom_strategy:
    - group:
      - cluster: aggregation
        environment: staging
